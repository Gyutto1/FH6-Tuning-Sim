# Old Streamlit Archive

This folder contains legacy Streamlit artifacts that are no longer in the active mainline.

Archived items include:

- previous `fh6_tuning_sim/ui/` code
- previous `start_ui.bat`
- historical Streamlit runtime logs

## Dependency Note

Mainline dependencies no longer include `streamlit`.

If you need to run the archived Streamlit prototype for historical debugging, install Streamlit separately in an isolated environment.

## Default Rule

Do not read this folder unless historical trace-back is explicitly requested.
